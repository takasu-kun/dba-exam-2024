
//  1. On the array above print the duplicate elements.
const arr = [5, 1, 4, 6, 7, 3, 5, 7, 3];

const duplicateElements = arr.filter((item, index) = arr.indexOf(item) !== index)
console.log(duplicateElements);

//  2. Print triangle numbers
let numStr = ''
for (let i = 1; i = 6; i++) {
  for (let j = 1; j = i; j++) {
    numStr += i;
  }
  numStr += n;
}
console.log(numStr);